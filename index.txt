<!doctype html>
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1, viewport-fit=cover" />
  <title>Revolut-Style Mobile Banking (HTML/CSS Prototype)</title>
  <style>
    :root{
      /* Primary Colors */
      --revolut-black:#191C1F;
      --revolut-dark:#1A1A1A;
      --revolut-charcoal:#2D2D2D;
      --revolut-gray-900:#1E1E1E;
      --revolut-gray-800:#2A2A2A;
      --revolut-gray-700:#3D3D3D;
      --revolut-gray-600:#4A4A4A;
      --revolut-gray-500:#6B6B6B;
      --revolut-gray-400:#8E8E93;
      --revolut-gray-300:#AEAEB2;
      --revolut-gray-200:#D1D1D6;
      --revolut-gray-100:#E5E5EA;
      --revolut-white:#FFFFFF;

      /* Accent Colors */
      --revolut-blue:#0075EB;
      --revolut-blue-light:#3D9DF3;
      --revolut-purple:#8B5CF6;
      --revolut-pink:#EC4899;
      --revolut-green:#00D09C;
      --revolut-green-dark:#00B386;
      --revolut-red:#FF3B30;
      --revolut-orange:#FF9500;
      --revolut-yellow:#FFCC00;

      /* Gradients */
      --gradient-premium:linear-gradient(135deg,#667eea 0%,#764ba2 100%);
      --gradient-card:linear-gradient(145deg,#2D2D2D 0%,#1A1A1A 100%);
      --gradient-metal:linear-gradient(135deg,#434343 0%,#000000 100%);

      /* Typography */
      --font-primary:'SF Pro Display', -apple-system, BlinkMacSystemFont, 'Inter', 'Segoe UI', sans-serif;
      --font-mono:'SF Mono','Fira Code',monospace;
      --text-xs:11px;
      --text-sm:13px;
      --text-base:15px;
      --text-lg:17px;
      --text-xl:20px;
      --text-2xl:24px;
      --text-3xl:28px;
      --text-4xl:34px;
      --text-5xl:42px;
      --text-balance:52px;

      --font-regular:400;
      --font-medium:500;
      --font-semibold:600;
      --font-bold:700;

      --tracking-tight:-0.02em;

      /* Spacing */
      --space-1:4px; --space-2:8px; --space-3:12px; --space-4:16px; --space-5:20px; --space-6:24px;
      --space-8:32px; --space-10:40px; --space-12:48px; --space-16:64px;

      /* Radius */
      --radius-sm:8px;
      --radius-md:12px;
      --radius-lg:16px;
      --radius-xl:20px;
      --radius-2xl:24px;
      --radius-full:9999px;

      /* Safe areas */
      --safe-area-top: env(safe-area-inset-top, 44px);
      --safe-area-bottom: env(safe-area-inset-bottom, 34px);

      /* Shadows */
      --shadow-card:0 8px 32px rgba(0,0,0,0.12);
      --shadow-elevated:0 20px 40px rgba(0,0,0,0.15);

      /* Blur */
      --blur-backdrop: blur(20px);
    }

    *{ box-sizing:border-box; }
    html,body{ height:100%; }
    body{
      margin:0;
      background: radial-gradient(1000px 700px at 20% 15%, rgba(0,117,235,0.10), transparent 50%),
                  radial-gradient(900px 650px at 85% 30%, rgba(139,92,246,0.12), transparent 55%),
                  var(--revolut-black);
      color: var(--revolut-white);
      font-family: var(--font-primary);
      letter-spacing: var(--tracking-tight);
      -webkit-font-smoothing: antialiased;
      -moz-osx-font-smoothing: grayscale;
      overflow-x:hidden;
    }

    /* Centered mobile container aesthetic */
    .app-shell{
      min-height:100%;
      display:flex;
      align-items:stretch;
      justify-content:center;
      padding: var(--space-6) var(--space-4);
    }

    .phone{
      width: min(480px, 100%);
      background: rgba(25,28,31,0.92);
      border: 1px solid rgba(255,255,255,0.06);
      border-radius: var(--radius-2xl);
      box-shadow: var(--shadow-elevated);
      overflow:hidden;
      position:relative;
    }

    /* Optional “device” top shine */
    .phone::before{
      content:"";
      position:absolute; inset:0;
      background: radial-gradient(800px 300px at 50% -10%, rgba(255,255,255,0.10), transparent 60%);
      pointer-events:none;
    }

    /* Screens */
    .screen{
      display:none;
      min-height: calc(100dvh - (var(--space-6) * 2));
      padding-top: var(--safe-area-top);
      padding-bottom: var(--safe-area-bottom);
      position:relative;
    }

    /* Default screen (no hash) */
    body:not(:has(.screen:target)) #login { display:block; }

    /* When targeted */
    .screen:target{
      display:block;
      animation: slideInRight 260ms ease-out both;
    }

    /* Ensure a selected screen appears even if default rule shows login */
    body:has(.screen:target) #login { display:none; }
    body:has(#login:target) #login { display:block; }

    @keyframes slideInRight{
      from { transform: translateX(14px); opacity:0; }
      to   { transform: translateX(0); opacity:1; }
    }

    @media (prefers-reduced-motion: reduce){
      .screen:target{ animation:none; }
      .pressable:active{ transform:none !important; }
      .orb{ animation:none !important; }
      .pulse{ animation:none !important; }
      .skeleton{ animation:none !important; }
    }

    /* Shared layout */
    .content{
      padding: 0 var(--space-4);
    }
    .stack{ display:flex; flex-direction:column; gap: var(--space-4); }
    .row{ display:flex; align-items:center; }
    .space-between{ justify-content:space-between; }
    .muted{ color: rgba(255,255,255,0.62); }
    .tiny{ font-size: var(--text-xs); }
    .sm{ font-size: var(--text-sm); }
    .base{ font-size: var(--text-base); }
    .lg{ font-size: var(--text-lg); }
    .xl{ font-size: var(--text-xl); }
    .h1{ font-size: var(--text-3xl); font-weight: var(--font-bold); }
    .balance{
      font-size: clamp(40px, 9.5vw, var(--text-balance));
      font-weight: var(--font-bold);
      letter-spacing: -0.03em;
      line-height:1.05;
    }

    /* Cards / surfaces */
    .card{
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.08);
      border-radius: var(--radius-xl);
      box-shadow: var(--shadow-card);
    }
    .card.pad{ padding: var(--space-4); }

    .pressable{
      -webkit-tap-highlight-color: transparent;
      touch-action: manipulation;
      user-select:none;
      transform-origin:center;
      transition: transform 100ms ease, background-color 120ms ease, border-color 120ms ease, box-shadow 120ms ease;
    }
    .pressable:active{ transform: scale(0.97); }

    .btn-primary{
      width:100%;
      border:0;
      border-radius: var(--radius-xl);
      padding: 14px 16px;
      font-weight: var(--font-semibold);
      font-size: var(--text-base);
      color: white;
      background: linear-gradient(135deg, var(--revolut-blue) 0%, #1b9cff 100%);
      box-shadow: 0 10px 22px rgba(0,117,235,0.25);
      text-decoration:none;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
    }

    .btn-ghost{
      width:100%;
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.10);
      color: rgba(255,255,255,0.90);
      border-radius: var(--radius-xl);
      padding: 14px 16px;
      font-weight: var(--font-semibold);
      font-size: var(--text-base);
      text-decoration:none;
      display:inline-flex;
      align-items:center;
      justify-content:center;
      gap:10px;
    }

    .link{
      color: rgba(255,255,255,0.80);
      text-decoration:none;
    }
    .link:hover{ text-decoration:underline; }

    /* Input */
    .input{
      width:100%;
      padding: 14px 14px;
      border-radius: var(--radius-lg);
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(0,0,0,0.25);
      color: white;
      font-size: var(--text-base);
      outline:none;
    }
    .input::placeholder{ color: rgba(255,255,255,0.40); }
    .input:focus{
      border-color: rgba(0,117,235,0.55);
      box-shadow: 0 0 0 3px rgba(0,117,235,0.18);
    }

    /* Login background orbs */
    .login-bg{
      position:absolute; inset:0;
      pointer-events:none;
      overflow:hidden;
    }
    .orb{
      position:absolute;
      filter: blur(26px);
      opacity: 0.55;
      border-radius: 999px;
      animation: float 6s ease-in-out infinite;
    }
    .orb.one{ width:260px; height:260px; left:-90px; top:110px; background: rgba(0,117,235,0.55); animation-delay: 0.2s; }
    .orb.two{ width:240px; height:240px; right:-120px; top:220px; background: rgba(139,92,246,0.55); animation-delay: 1.0s; }
    .orb.three{ width:210px; height:210px; left:40px; bottom:-110px; background: rgba(236,72,153,0.35); animation-delay: 1.6s; }
    @keyframes float{
      0%,100%{ transform: translateY(0) translateX(0) scale(1); }
      50%{ transform: translateY(-14px) translateX(8px) scale(1.04); }
    }

    /* Logo */
    .logo-wrap{
      display:flex; justify-content:center;
      padding-top: var(--space-8);
      padding-bottom: var(--space-2);
    }
    .logo{
      width:56px; height:56px;
      border-radius: 18px;
      background: linear-gradient(135deg, rgba(255,255,255,0.16), rgba(255,255,255,0.04));
      border: 1px solid rgba(255,255,255,0.10);
      display:flex; align-items:center; justify-content:center;
      font-weight:800;
      letter-spacing:-0.03em;
      box-shadow: 0 14px 30px rgba(0,0,0,0.25);
      transform: translateY(0);
      animation: logoIn 420ms cubic-bezier(.2,.9,.2,1) both;
    }
    @keyframes logoIn{
      from{ opacity:0; transform: translateY(-10px) scale(0.96); }
      to{ opacity:1; transform: translateY(0) scale(1); }
    }

    /* PIN dots + keypad (CSS-only mock) */
    .pin-dots{ display:flex; gap:10px; justify-content:center; padding: var(--space-6) 0 var(--space-2); }
    .dot{
      width:10px; height:10px; border-radius: 999px;
      background: rgba(255,255,255,0.20);
      border: 1px solid rgba(255,255,255,0.12);
    }
    /* Demo “filled” look (static) */
    .dot.filled{ background: rgba(255,255,255,0.88); }

    .keypad{
      display:grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 12px;
      padding: var(--space-4);
    }
    .key{
      height: 56px;
      border-radius: 18px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
      font-size: 18px;
      font-weight: var(--font-semibold);
      color: rgba(255,255,255,0.92);
      text-decoration:none;
    }
    .key:active{ background: rgba(255,255,255,0.10); transform: scale(0.98); }

    .biometric{
      display:flex; align-items:center; justify-content:center; gap:10px;
      padding: var(--space-3) 0 var(--space-6);
    }
    .faceid{
      width:44px; height:44px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,0.12);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
    }
    .pulse{ position:relative; }
    .pulse::after{
      content:"";
      position:absolute; inset:-8px;
      border-radius: 16px;
      border: 1px solid rgba(0,208,156,0.35);
      opacity: 0;
      animation: pulse 1.4s ease-out infinite;
    }
    @keyframes pulse{
      0%{ transform: scale(0.92); opacity:0; }
      35%{ opacity: 0.7; }
      100%{ transform: scale(1.12); opacity: 0; }
    }

    /* Header */
    .topbar{
      padding: 8px var(--space-4) var(--space-2);
      display:flex;
      align-items:center;
      justify-content:space-between;
      gap: 12px;
    }
    .avatar{
      width:36px; height:36px;
      border-radius: var(--radius-full);
      background: linear-gradient(135deg, rgba(255,255,255,0.20), rgba(255,255,255,0.06));
      border: 1px solid rgba(255,255,255,0.10);
      display:flex; align-items:center; justify-content:center;
      font-weight: var(--font-semibold);
      color: rgba(255,255,255,0.9);
      flex: 0 0 auto;
    }
    .gear{
      width:36px; height:36px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
      text-decoration:none;
      color: rgba(255,255,255,0.88);
      flex: 0 0 auto;
    }

    .account-pill{
      display:inline-flex; align-items:center; justify-content:center; gap:8px;
      margin: 10px auto 0;
      padding: 8px 12px;
      width: fit-content;
      border-radius: var(--radius-full);
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      color: rgba(255,255,255,0.76);
      font-size: var(--text-sm);
      text-decoration:none;
    }

    /* Quick actions */
    .quick-actions{
      display:flex;
      gap: 14px;
      justify-content:space-between;
      padding: 0 var(--space-4);
    }
    .qa{
      flex:1 1 0;
      display:flex; flex-direction:column; align-items:center; gap:8px;
      text-decoration:none;
      color: rgba(255,255,255,0.92);
    }
    .qa .icon{
      width:52px; height:52px;
      border-radius: 18px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
      box-shadow: 0 10px 18px rgba(0,0,0,0.18);
    }
    .qa .label{ font-size: var(--text-sm); color: rgba(255,255,255,0.78); }

    /* Card carousel */
    .section-head{
      padding: 0 var(--space-4);
      display:flex; align-items:center; justify-content:space-between;
      margin-top: var(--space-6);
      margin-bottom: var(--space-2);
    }
    .section-title{ font-size: var(--text-lg); font-weight: var(--font-semibold); }
    .section-link{ font-size: var(--text-sm); color: rgba(255,255,255,0.70); text-decoration:none; }

    .carousel{
      padding: 0 var(--space-4) var(--space-2);
      display:flex;
      gap: 14px;
      overflow-x:auto;
      scroll-snap-type: x mandatory;
      -webkit-overflow-scrolling: touch;
    }
    .carousel::-webkit-scrollbar{ height:0; }

    .vcard{
      width: 280px;
      height: 175px;
      flex: 0 0 auto;
      border-radius: var(--radius-lg);
      scroll-snap-align: start;
      position:relative;
      overflow:hidden;
      border: 1px solid rgba(255,255,255,0.12);
      box-shadow: 0 16px 34px rgba(0,0,0,0.24);
    }
    .vcard.standard{ background: var(--gradient-card); }
    .vcard.premium{ background: var(--gradient-premium); }
    .vcard.metal{ background: var(--gradient-metal); }

    .vcard::after{
      content:"";
      position:absolute; inset:-60px;
      background: radial-gradient(closest-side at 30% 30%, rgba(255,255,255,0.20), transparent 60%);
      transform: rotate(8deg);
      pointer-events:none;
    }
    .vcard-inner{
      position:absolute; inset:0;
      padding: 14px 14px;
      display:flex; flex-direction:column;
      justify-content:space-between;
    }
    .vcard-top{
      display:flex; align-items:center; justify-content:space-between;
      color: rgba(255,255,255,0.90);
      font-weight: var(--font-semibold);
    }
    .vcard-logo{
      display:flex; align-items:center; gap:8px;
      font-size: 14px;
    }
    .chip{
      width:34px; height:26px;
      border-radius: 8px;
      background: linear-gradient(135deg, rgba(255,255,255,0.22), rgba(255,255,255,0.06));
      border: 1px solid rgba(255,255,255,0.16);
    }
    .vcard-num{ font-family: var(--font-mono); font-size: 13px; opacity:0.9; }
    .vcard-bottom{
      display:flex; align-items:flex-end; justify-content:space-between;
      color: rgba(255,255,255,0.90);
    }
    .vcard-meta{ display:flex; flex-direction:column; gap:6px; }
    .vcard-name{ font-size: 12px; opacity:0.85; }
    .vcard-exp{ font-size: 12px; opacity:0.8; }
    .brand{
      font-weight: 800;
      letter-spacing: 0.02em;
      opacity: 0.85;
    }

    /* Transactions preview/list */
    .list{
      margin: 0 var(--space-4);
      border-radius: var(--radius-xl);
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.04);
      overflow:hidden;
    }
    .item{
      display:flex;
      align-items:center;
      gap: 12px;
      padding: 14px 14px;
      text-decoration:none;
      color: white;
    }
    .item + .item{ border-top: 1px solid rgba(255,255,255,0.08); }
    .item:active{ background: rgba(255,255,255,0.06); }
    .i-icon{
      width:44px; height:44px;
      border-radius: var(--radius-full);
      background: rgba(255,255,255,0.06);
      border: 1px solid rgba(255,255,255,0.10);
      display:flex; align-items:center; justify-content:center;
      font-size: 20px;
      flex: 0 0 auto;
    }
    .i-mid{ flex:1; min-width:0; }
    .i-title{
      font-size: var(--text-base);
      font-weight: var(--font-medium);
      white-space:nowrap;
      overflow:hidden;
      text-overflow:ellipsis;
    }
    .i-sub{
      margin-top: 2px;
      font-size: var(--text-sm);
      color: rgba(255,255,255,0.60);
    }
    .amt{
      font-weight: var(--font-semibold);
      font-size: var(--text-base);
      white-space:nowrap;
    }
    .amt.pos{ color: var(--revolut-green); }
    .chev{
      color: rgba(255,255,255,0.35);
      margin-left: 4px;
    }

    /* Transactions page header */
    .page-head{
      padding: 8px var(--space-4) 10px;
      display:flex; align-items:center; justify-content:space-between; gap:10px;
    }
    .back{
      width:40px; height:40px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
      text-decoration:none;
      color: rgba(255,255,255,0.92);
    }
    .filters{
      display:flex; gap:10px;
    }
    .iconbtn{
      width:40px; height:40px;
      border-radius: 14px;
      border: 1px solid rgba(255,255,255,0.10);
      background: rgba(255,255,255,0.06);
      display:flex; align-items:center; justify-content:center;
      text-decoration:none;
      color: rgba(255,255,255,0.92);
    }

    .search{
      padding: 0 var(--space-4) var(--space-3);
    }

    .summary{
      margin: 0 var(--space-4) var(--space-4);
      border-radius: var(--radius-xl);
      padding: var(--space-4);
      border: 1px solid rgba(255,255,255,0.10);
      background: linear-gradient(145deg, rgba(255,255,255,0.06), rgba(255,255,255,0.03));
      box-shadow: var(--shadow-card);
    }
    .summary .big{ font-size: var(--text-2xl); font-weight: var(--font-bold); margin-top: 6px; }
    .summary .delta{ color: rgba(0,208,156,0.92); font-size: var(--text-sm); margin-top: 6px; }

    .group-title{
      padding: 8px var(--space-4) 8px;
      color: rgba(255,255,255,0.62);
      font-size: var(--text-sm);
      font-weight: var(--font-medium);
    }

    /* Bottom navigation */
    .bottom-nav{
      position: sticky;
      bottom: 0;
      padding-bottom: var(--safe-area-bottom);
      background: rgba(26,26,26,0.92);
      border-top: 1px solid rgba(255,255,255,0.10);
      backdrop-filter: var(--blur-backdrop);
      -webkit-backdrop-filter: var(--blur-backdrop);
    }
    .tabs{
      height: 64px;
      display:flex;
      align-items:center;
      justify-content:space-around;
    }
    .tab{
      width: 64px;
      height: 54px;
      display:flex;
      flex-direction:column;
      align-items:center;
      justify-content:center;
      gap: 6px;
      text-decoration:none;
      color: rgba(255,255,255,0.55);
      font-size: var(--text-xs);
    }
    .tab .ticon{ font-size: 20px; line-height:1; }
    .tab.active{ color: var(--revolut-blue-light); }
    .tab .label{ display:none; }
    .tab.active .label{ display:block; }

    /* Skeleton (optional demo block) */
    .skeleton{
      position:relative;
      overflow:hidden;
      background: rgba(255,255,255,0.06);
    }
    .skeleton::after{
      content:"";
      position:absolute; inset:0;
      transform: translateX(-100%);
      background: linear-gradient(90deg, transparent, rgba(255,255,255,0.10), transparent);
      animation: shimmer 1.4s infinite;
    }
    @keyframes shimmer{
      to{ transform: translateX(100%); }
    }

    /* Helpers */
    .center{ text-align:center; }
    .mt2{ margin-top: var(--space-2); }
    .mt4{ margin-top: var(--space-4); }
    .mt6{ margin-top: var(--space-6); }
    .pb-nav{ padding-bottom: calc(64px + var(--safe-area-bottom) + var(--space-4)); }
    .divider{ height:1px; background: rgba(255,255,255,0.08); margin: var(--space-4) 0; }
  </style>
</head>

<body>
  <div class="app-shell">
    <main class="phone" role="application" aria-label="Revolut-style mobile banking prototype">
      <!-- LOGIN -->
      <section id="login" class="screen" aria-label="Login">
        <div class="login-bg" aria-hidden="true">
          <div class="orb one"></div>
          <div class="orb two"></div>
          <div class="orb three"></div>
        </div>

        <div class="logo-wrap">
          <div class="logo" aria-label="Revolut logo">R</div>
        </div>

        <div class="content stack" style="position:relative;">
          <div class="center">
            <div class="h1">Welcome back</div>
            <div class="muted base mt2">Sign in to continue</div>
          </div>

          <div class="card pad">
            <label class="sm muted" for="phone">Phone number</label>
            <div class="row" style="gap:10px; margin-top:10px;">
              <div class="card" style="padding:12px 12px; border-radius:16px; background:rgba(255,255,255,0.06); border:1px solid rgba(255,255,255,0.10);">
                🇬🇧 <span class="muted sm" style="margin-left:6px;">+44</span>
              </div>
              <input id="phone" class="input" inputmode="tel" placeholder="7700 900000" aria-label="Phone number" />
            </div>

            <a class="btn-primary pressable mt4" href="#pin" aria-label="Continue to PIN">
              Continue
              <span aria-hidden="true">→</span>
            </a>

            <div class="row space-between mt4">
              <a class="link sm" href="#pin">Forgot PIN?</a>
              <a class="link sm" href="#login">Create account</a>
            </div>
          </div>

          <div class="center muted tiny" style="padding: 0 28px;">
            Prototype note: this is a CSS-only flow. Tap “Continue” to view the PIN screen.
          </div>
        </div>
      </section>

      <!-- PIN -->
      <section id="pin" class="screen" aria-label="PIN entry">
        <div class="content">
          <div class="row space-between" style="padding: 8px 0 2px;">
            <a class="back pressable" href="#login" aria-label="Back to login">←</a>
            <div class="muted sm">Secure sign in</div>
            <div style="width:40px;"></div>
          </div>

          <div class="center mt6">
            <div class="xl" style="font-weight: var(--font-semibold);">Enter your PIN</div>
            <div class="muted sm mt2">Use Face ID or type your 6-digit PIN</div>
          </div>

          <div class="pin-dots" aria-label="PIN dots (demo)">
            <div class="dot filled"></div>
            <div class="dot filled"></div>
            <div class="dot filled"></div>
            <div class="dot"></div>
            <div class="dot"></div>
            <div class="dot"></div>
          </div>

          <div class="biometric">
            <div class="faceid pulse" aria-hidden="true">🙂</div>
            <span class="muted sm">Face ID</span>
          </div>

          <div class="keypad" aria-label="Numeric keypad (static)">
            <a class="key pressable" href="#pin" aria-label="Key 1">1</a>
            <a class="key pressable" href="#pin" aria-label="Key 2">2</a>
            <a class="key pressable" href="#pin" aria-label="Key 3">3</a>
            <a class="key pressable" href="#pin" aria-label="Key 4">4</a>
            <a class="key pressable" href="#pin" aria-label="Key 5">5</a>
            <a class="key pressable" href="#pin" aria-label="Key 6">6</a>
            <a class="key pressable" href="#pin" aria-label="Key 7">7</a>
            <a class="key pressable" href="#pin" aria-label="Key 8">8</a>
            <a class="key pressable" href="#pin" aria-label="Key 9">9</a>
            <a class="key pressable" href="#pin" aria-label="Key star">*</a>
            <a class="key pressable" href="#pin" aria-label="Key 0">0</a>
            <a class="key pressable" href="#pin" aria-label="Key backspace">⌫</a>
          </div>

          <div class="content" style="padding: 0; padding-bottom: var(--space-6);">
            <a class="btn-primary pressable" href="#home" aria-label="Demo success: go to Home">
              Demo Success (PIN 000000) ✓
            </a>
            <a class="btn-ghost pressable mt2" href="#pin" aria-label="Demo error: incorrect PIN">
              Demo Error (shake state placeholder)
            </a>
            <div class="center muted tiny mt2">
              (Validation needs JavaScript; this is a visual prototype.)
            </div>
          </div>
        </div>
      </section>

      <!-- HOME -->
      <section id="home" class="screen pb-nav" aria-label="Home dashboard">
        <header class="topbar">
          <div class="row" style="gap:12px; min-width:0;">
            <div class="avatar" aria-label="User avatar">JD</div>
            <div style="min-width:0;">
              <div class="muted sm">Good morning,</div>
              <div class="lg" style="font-weight: var(--font-semibold); white-space:nowrap; overflow:hidden; text-overflow:ellipsis;">
                John
              </div>
            </div>
          </div>
          <a class="gear pressable" href="#home" aria-label="Settings">⚙️</a>
        </header>

        <div class="content center" style="padding-top: 6px;">
          <div class="balance" aria-label="Account balance">£12,450.00</div>
          <a class="account-pill pressable" href="#home" aria-label="Account selector">
            Main · GBP <span aria-hidden="true">▾</span>
          </a>
        </div>

        <div class="mt6">
          <nav class="quick-actions" aria-label="Quick actions">
            <a class="qa pressable" href="#home" aria-label="Send">
              <div class="icon" aria-hidden="true">↗️</div>
              <div class="label">Send</div>
            </a>
            <a class="qa pressable" href="#home" aria-label="Request">
              <div class="icon" aria-hidden="true">↘️</div>
              <div class="label">Request</div>
            </a>
            <a class="qa pressable" href="#home" aria-label="Add money">
              <div class="icon" aria-hidden="true">➕</div>
              <div class="label">Add</div>
            </a>
            <a class="qa pressable" href="#home" aria-label="More">
              <div class="icon" aria-hidden="true">⋯</div>
              <div class="label">More</div>
            </a>
          </nav>
        </div>

        <div class="section-head">
          <div class="section-title">Your cards</div>
          <a class="section-link" href="#home" aria-label="Manage cards">Manage</a>
        </div>

        <div class="carousel" aria-label="Card carousel">
          <div class="vcard standard pressable" role="button" tabindex="0" aria-label="Standard card ending 4242">
            <div class="vcard-inner">
              <div class="vcard-top">
                <div class="vcard-logo"><span style="font-weight:800;">R</span> Revolut</div>
                <div class="chip" aria-hidden="true"></div>
              </div>
              <div class="vcard-num">•••• 4242</div>
              <div class="vcard-bottom">
                <div class="vcard-meta">
                  <div class="vcard-name">JOHN DOE</div>
                  <div class="vcard-exp">12/27</div>
                </div>
                <div class="brand" aria-label="Card network">VISA</div>
              </div>
            </div>
          </div>

          <div class="vcard premium pressable" role="button" tabindex="0" aria-label="Premium card ending 8888">
            <div class="vcard-inner">
              <div class="vcard-top">
                <div class="vcard-logo"><span style="font-weight:800;">R</span> Premium</div>
                <div class="chip" aria-hidden="true"></div>
              </div>
              <div class="vcard-num">•••• 8888</div>
              <div class="vcard-bottom">
                <div class="vcard-meta">
                  <div class="vcard-name">JOHN DOE</div>
                  <div class="vcard-exp">06/26</div>
                </div>
                <div class="brand">MC</div>
              </div>
            </div>
          </div>

          <div class="vcard metal pressable" role="button" tabindex="0" aria-label="Metal card ending 1010">
            <div class="vcard-inner">
              <div class="vcard-top">
                <div class="vcard-logo"><span style="font-weight:800;">R</span> Metal</div>
                <div class="chip" aria-hidden="true"></div>
              </div>
              <div class="vcard-num">•••• 1010</div>
              <div class="vcard-bottom">
                <div class="vcard-meta">
                  <div class="vcard-name">JOHN DOE</div>
                  <div class="vcard-exp">11/28</div>
                </div>
                <div class="brand">VISA</div>
              </div>
            </div>
          </div>
        </div>

        <div class="section-head">
          <div class="section-title">Recent transactions</div>
          <a class="section-link" href="#transactions" aria-label="See all transactions">See all</a>
        </div>

        <div class="list" aria-label="Recent transactions list">
          <a class="item pressable" href="#transactions" aria-label="Tesco Express transaction">
            <div class="i-icon" aria-hidden="true">🛒</div>
            <div class="i-mid">
              <div class="i-title">Tesco Express</div>
              <div class="i-sub">Groceries · 14:32</div>
            </div>
            <div class="amt">-£45.20</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>

          <a class="item pressable" href="#transactions" aria-label="Transfer from John Doe">
            <div class="i-icon" aria-hidden="true">↓</div>
            <div class="i-mid">
              <div class="i-title">John Doe</div>
              <div class="i-sub">Transfer · 18:45</div>
            </div>
            <div class="amt pos">+£150.00</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>

          <a class="item pressable" href="#transactions" aria-label="Starbucks transaction">
            <div class="i-icon" aria-hidden="true">☕</div>
            <div class="i-mid">
              <div class="i-title">Starbucks</div>
              <div class="i-sub">Restaurants · 09:15</div>
            </div>
            <div class="amt">-£4.95</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>
        </div>

        <div class="content mt6">
          <div class="card pad skeleton" style="height: 84px;" aria-label="Skeleton loading demo"></div>
        </div>

        <footer class="bottom-nav" aria-label="Bottom navigation">
          <nav class="tabs">
            <a class="tab active pressable" href="#home" aria-label="Home tab">
              <div class="ticon" aria-hidden="true">🏠</div>
              <div class="label">Home</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Cards tab (prototype)">
              <div class="ticon" aria-hidden="true">💳</div>
              <div class="label">Cards</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Analytics tab (prototype)">
              <div class="ticon" aria-hidden="true">📊</div>
              <div class="label">Analytics</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Exchange tab (prototype)">
              <div class="ticon" aria-hidden="true">💱</div>
              <div class="label">Exchange</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Profile tab (prototype)">
              <div class="ticon" aria-hidden="true">👤</div>
              <div class="label">Profile</div>
            </a>
          </nav>
        </footer>
      </section>

      <!-- TRANSACTIONS -->
      <section id="transactions" class="screen pb-nav" aria-label="Transactions">
        <header class="page-head">
          <a class="back pressable" href="#home" aria-label="Back to Home">←</a>
          <div style="font-weight: var(--font-semibold);">Transactions</div>
          <div class="filters" aria-label="Search and filter">
            <a class="iconbtn pressable" href="#transactions" aria-label="Search">🔍</a>
            <a class="iconbtn pressable" href="#transactions" aria-label="Filters">🎚️</a>
          </div>
        </header>

        <div class="search">
          <input class="input" placeholder="Search merchant, category…" aria-label="Search transactions" />
        </div>

        <div class="summary" aria-label="Balance summary">
          <div class="muted sm">Total Balance</div>
          <div class="big">£12,450.00</div>
          <div class="delta">↑ £1,200.00 this month</div>
        </div>

        <div class="group-title">Today</div>
        <div class="list" aria-label="Today transactions">
          <a class="item pressable" href="#transactions" aria-label="Tesco Express today">
            <div class="i-icon" aria-hidden="true">🛒</div>
            <div class="i-mid">
              <div class="i-title">Tesco Express</div>
              <div class="i-sub">Groceries · 14:32</div>
            </div>
            <div class="amt">-£45.20</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>

          <a class="item pressable" href="#transactions" aria-label="Starbucks today">
            <div class="i-icon" aria-hidden="true">☕</div>
            <div class="i-mid">
              <div class="i-title">Starbucks</div>
              <div class="i-sub">Restaurants · 09:15</div>
            </div>
            <div class="amt">-£4.95</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>
        </div>

        <div class="group-title">Yesterday</div>
        <div class="list" aria-label="Yesterday transactions">
          <a class="item pressable" href="#transactions" aria-label="Transfer from John Doe yesterday">
            <div class="i-icon" aria-hidden="true">↓</div>
            <div class="i-mid">
              <div class="i-title">John Doe</div>
              <div class="i-sub">Transfer · 18:45</div>
            </div>
            <div class="amt pos">+£150.00</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>

          <a class="item pressable" href="#transactions" aria-label="Deliveroo yesterday">
            <div class="i-icon" aria-hidden="true">🍕</div>
            <div class="i-mid">
              <div class="i-title">Deliveroo</div>
              <div class="i-sub">Restaurants · 20:30</div>
            </div>
            <div class="amt">-£24.50</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>
        </div>

        <div class="group-title">Last week</div>
        <div class="list" aria-label="Last week transactions">
          <a class="item pressable" href="#transactions" aria-label="Apple transaction">
            <div class="i-icon" aria-hidden="true"></div>
            <div class="i-mid">
              <div class="i-title">Apple</div>
              <div class="i-sub">Subscriptions · 11:02</div>
            </div>
            <div class="amt">-£9.99</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>

          <a class="item pressable" href="#transactions" aria-label="Salary payment">
            <div class="i-icon" aria-hidden="true">💼</div>
            <div class="i-mid">
              <div class="i-title">Acme Payroll</div>
              <div class="i-sub">Salary · 08:00</div>
            </div>
            <div class="amt pos">+£2,450.00</div>
            <div class="chev" aria-hidden="true">›</div>
          </a>
        </div>

        <footer class="bottom-nav" aria-label="Bottom navigation">
          <nav class="tabs">
            <a class="tab pressable" href="#home" aria-label="Home tab">
              <div class="ticon" aria-hidden="true">🏠</div>
              <div class="label">Home</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Cards tab (prototype)">
              <div class="ticon" aria-hidden="true">💳</div>
              <div class="label">Cards</div>
            </a>
            <a class="tab active pressable" href="#transactions" aria-label="Transactions tab">
              <div class="ticon" aria-hidden="true">📊</div>
              <div class="label">Analytics</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Exchange tab (prototype)">
              <div class="ticon" aria-hidden="true">💱</div>
              <div class="label">Exchange</div>
            </a>
            <a class="tab pressable" href="#home" aria-label="Profile tab (prototype)">
              <div class="ticon" aria-hidden="true">👤</div>
              <div class="label">Profile</div>
            </a>
          </nav>
        </footer>
      </section>
    </main>
  </div>

  <!--
    Usage:
    1) Save as index.html
    2) Open in a mobile browser (or Chrome DevTools mobile emulation)
    3) Navigate via:
       - Login -> Continue -> PIN -> Demo Success -> Home
       - Home -> See all -> Transactions
  -->
</body>
</html>
